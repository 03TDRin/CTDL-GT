#include <iostream>
using namespace std;
float sqrt3(float x){
  if(x==0){
    return 0;
    }
  if(x<0){
    return(-sqrt3(-x));
    return(exp(log(x)/3));
    }
int main()
{
    float x;
    cout<<"Nhap x:";
    cin>>n;
    x=sqrt3(x);
    cout<<"Ket qua la : %2f "<< x <<endl;
    return 0;
}
